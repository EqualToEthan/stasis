# 气隙架构：完全离线 + JSON 协议交换

冷钱包采用气隙（air-gap）安全模型：签名端完全离线，与联网端通过 JSON 数据载体交换。选择二维码和文件导出/导入作为传输通道，排除蓝牙、NFC、Wi-Fi Direct 等任何需要无线电信号直连的方案。

这个决策的核心权衡是：牺牲用户体验的便利性，换取私钥永远不接触任何联网设备的绝对安全保证。气隙是冷钱包安全等级最高的隔离方式，一旦放松（如引入蓝牙），攻击面会非线性扩大。
