Flutter (Material3) + Dart SDK ^3.11.1；使用 `cardano_flutter_sdk` 与 `cardano_dart_types` 进行 Cardano 交易构建，`http` 调用 Blockfrost v0 REST API，`mobile_scanner` + `qr_flutter` 实现二维码扫描/展示，`flutter_secure_storage` 安全保存 API Key，`shared_preferences` 持久化钱包列表。