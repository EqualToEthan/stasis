Flutter (SDK ^3.11.1) + Material3；Cardano 密码学通过 `cardano_flutter_sdk` 4.x 与 `cardano_dart_types` 实现 BIP-39 助记词、CIP-1852 路径派生与 CBOR 交易处理；助记词校验使用 `bip39_plus`；密钥与 PIN 通过 `flutter_secure_storage` 11.x 安全存储；二维码扫描使用 `mobile_scanner` 7.x，QR 展示使用 `qr_flutter`；文件导入使用 `file_picker` 12 beta。